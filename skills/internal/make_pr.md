# Pre-PR contract

Before a PR or shared commit: read `skills/internal/review_round.md`, run the fast preflight,
perform a whole-arc review round, resolve confirmed findings in one batch, re-review that batch,
then run the full applicable preflight.  Record known-red corpus state honestly; it cannot be
described as a passing validation result.
