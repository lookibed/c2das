# TDD audit

Use the `tdd-auditor` contract.  A changed reachable lowering branch needs a fixture that fails
when the branch is disabled or reverted.  Negative controls are allowed only in an isolated
worktree and must be restored and proven restored before reporting.
