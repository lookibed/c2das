# Worktree audit

Create a dedicated WSL Git worktree under `/root/` for mutation-based audit. Do not mutate the
implementation worktree `/root/c2das`. Record worktree path, narrow command, restoration command,
and clean `git diff` proof.
