# WSL and CI reproduction

Canonical local runtime evidence is the native Git checkout `/root/c2das` and
`/root/daScript/bin/daslang`. Linux CI runs the same shell preflight with a pinned daScript build.
Windows runtime output is informational only.
