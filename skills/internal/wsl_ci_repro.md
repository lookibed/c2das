# WSL and CI reproduction

Canonical local runtime evidence is a hash-verified named WSL mirror and `/root/daScript/bin/daslang`.
Linux CI runs the same shell preflight with a pinned daScript build.  Windows runtime output is
informational until a stable default-runtime gate exists.
