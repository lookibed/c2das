#!/usr/bin/env bash
set -e

if [[ -x /usr/bin/intercept-build ]]; then
    exec /usr/bin/intercept-build "$@"
fi

if command -v bear >/dev/null 2>&1; then
    exec bear -- "$@"
fi

echo "intercept-build compatibility wrapper requires /usr/bin/intercept-build or bear" >&2
exit 127
