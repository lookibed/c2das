# system-wide conf

requirements:
  ubuntu: # for ubuntu hosts
    apt:
      packages:
        - build-essential

  generic: # for any host
    programs:
      in_path:
        - c2dascript-transpile
        - daslang
        - make
        - cmake
        - intercept-build
