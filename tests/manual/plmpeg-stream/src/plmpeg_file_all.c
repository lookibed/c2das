/* Translation input for the `--libc std` file case: the c2das target graph
 * (`all.c`) and the C entry over a stream file in one translation unit, so
 * the translated module carries the program's `main` and runs as is. */
#include "all.c"
#include "plmpeg_file_reference_entry.c"
