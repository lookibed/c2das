struct Point {
    int x;
    int y;
};

enum Color {
    RED,
    GREEN,
    BLUE = 5
};

int use_types() {
    struct Point p;
    p.x = 1;
    p.y = 2;
    enum Color c = RED;
    return p.x + p.y + c;
}
